<nav class="navbar navbar-expand-lg navbar-dark fixed-top scrolling-navbar">
  <div class="container">
    <a class="navbar-brand" href="{{ path_for('home') }}">
      <strong>{{ appName }}</strong>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent-7"
      aria-controls="navbarSupportedContent-7" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent-7">
      <ul class="navbar-nav mr-auto">
        {% if session.name is defined %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('home') }}">Home</a>
        </li>
        {% endif %}
        {% if session.name is defined %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('services') }}">Services</a>
        </li>
        {% endif %}
        {% if session.name is empty %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('signin.show') }}">Sign In</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('signup.show') }}">Sign Up</a>
        </li>
        {% endif %}
        {% if session.name is defined %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('logout') }}">Log Out</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>